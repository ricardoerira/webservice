@model IEnumerable<MvcApplication2.Areas.Api.Models.Departamento>

@{
    ViewBag.Title = "Index";
}

<h2>Index</h2>

<p>
    @Html.ActionLink("Create New", "Create")
</p>
<table>
    <tr>
        <th>
            @Html.DisplayNameFor(model => model.dep_nombre)
        </th>
        <th>
            @Html.DisplayNameFor(model => model.est_codigo)
        </th>
        <th>
            @Html.DisplayNameFor(model => model.Paises.pai_nombre)
        </th>
        <th></th>
    </tr>

@foreach (var item in Model) {
    <tr>
        <td>
            @Html.DisplayFor(modelItem => item.dep_nombre)
        </td>
        <td>
            @Html.DisplayFor(modelItem => item.est_codigo)
        </td>
        <td>
            @Html.DisplayFor(modelItem => item.Paises.pai_nombre)
        </td>
        <td>
            @Html.ActionLink("Edit", "Edit", new { id=item.departamentoId }) |
            @Html.ActionLink("Details", "Details", new { id=item.departamentoId }) |
            @Html.ActionLink("Delete", "Delete", new { id=item.departamentoId })
        </td>
    </tr>
}

</table>
