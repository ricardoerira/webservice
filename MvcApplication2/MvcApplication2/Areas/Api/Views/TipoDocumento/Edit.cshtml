@model MvcApplication2.Areas.Api.Models.TipoDocumento

@{
    ViewBag.Title = "Edit";
}

<h2>Edit</h2>

@using (Html.BeginForm()) {
    @Html.ValidationSummary(true)

    <fieldset>
        <legend>TipoDocumento</legend>

        @Html.HiddenFor(model => model.tipoDocumentoId)

        <div class="editor-label">
            @Html.LabelFor(model => model.tipo_doc)
        </div>
        <div class="editor-field">
            @Html.EditorFor(model => model.tipo_doc)
            @Html.ValidationMessageFor(model => model.tipo_doc)
        </div>

        <div class="editor-label">
            @Html.LabelFor(model => model.est_codigo)
        </div>
        <div class="editor-field">
            @Html.EditorFor(model => model.est_codigo)
            @Html.ValidationMessageFor(model => model.est_codigo)
        </div>

        <p>
            <input type="submit" value="Save" />
        </p>
    </fieldset>
}

<div>
    @Html.ActionLink("Back to List", "Index")
</div>

@section Scripts {
    @Scripts.Render("~/bundles/jqueryval")
}
