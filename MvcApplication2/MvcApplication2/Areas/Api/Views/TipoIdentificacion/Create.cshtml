@model MvcApplication2.Areas.Api.Models.TipoIdentificacion

@{
    ViewBag.Title = "Create";
}

<h2>Create</h2>

@using (Html.BeginForm()) {
    @Html.ValidationSummary(true)

    <fieldset>
        <legend>TipoIdentificacion</legend>

        <div class="editor-label">
            @Html.LabelFor(model => model.tide_nombre)
        </div>
        <div class="editor-field">
            @Html.EditorFor(model => model.tide_nombre)
            @Html.ValidationMessageFor(model => model.tide_nombre)
        </div>

        <div class="editor-label">
            @Html.LabelFor(model => model.est_codigo)
        </div>
        <div class="editor-field">
            @Html.EditorFor(model => model.est_codigo)
            @Html.ValidationMessageFor(model => model.est_codigo)
        </div>

        <p>
            <input type="submit" value="Create" />
        </p>
    </fieldset>
}

<div>
    @Html.ActionLink("Back to List", "Index")
</div>

@section Scripts {
    @Scripts.Render("~/bundles/jqueryval")
}
