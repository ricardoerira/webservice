@model MvcApplication2.Areas.Api.Models.TipoUsuario

@{
    ViewBag.Title = "Create";
}

<h2>Create</h2>

@using (Html.BeginForm()) {
    @Html.ValidationSummary(true)

    <fieldset>
        <legend>TipoUsuario</legend>

        <div class="editor-label">
            @Html.LabelFor(model => model.tusu_nombre)
        </div>
        <div class="editor-field">
            @Html.EditorFor(model => model.tusu_nombre)
            @Html.ValidationMessageFor(model => model.tusu_nombre)
        </div>

        <div class="editor-label">
            @Html.LabelFor(model => model.est_codigo)
        </div>
        <div class="editor-field">
            @Html.EditorFor(model => model.est_codigo)
            @Html.ValidationMessageFor(model => model.est_codigo)
        </div>

        <p>
            <input type="submit" value="Create" />
        </p>
    </fieldset>
}

<div>
    @Html.ActionLink("Back to List", "Index")
</div>

@section Scripts {
    @Scripts.Render("~/bundles/jqueryval")
}
