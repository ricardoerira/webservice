@model IEnumerable<MvcApplication2.Areas.Api.Models.Municipio>

@{
    ViewBag.Title = "Index";
}

<h2>Index</h2>

<p>
    @Html.ActionLink("Create New", "Create")
</p>
<table>
    <tr>
        <th>
            @Html.DisplayNameFor(model => model.mun_nombre)
        </th>
        <th>
            @Html.DisplayNameFor(model => model.est_codigo)
        </th>
        <th>
            @Html.DisplayNameFor(model => model.Departamentos.dep_nombre)
        </th>
        <th></th>
    </tr>

@foreach (var item in Model) {
    <tr>
        <td>
            @Html.DisplayFor(modelItem => item.mun_nombre)
        </td>
        <td>
            @Html.DisplayFor(modelItem => item.est_codigo)
        </td>
        <td>
            @Html.DisplayFor(modelItem => item.Departamentos.dep_nombre)
        </td>
        <td>
            @Html.ActionLink("Edit", "Edit", new { id=item.municipioId }) |
            @Html.ActionLink("Details", "Details", new { id=item.municipioId }) |
            @Html.ActionLink("Delete", "Delete", new { id=item.municipioId })
        </td>
    </tr>
}

</table>
