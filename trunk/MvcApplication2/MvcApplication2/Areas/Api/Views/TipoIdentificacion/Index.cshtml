@model IEnumerable<MvcApplication2.Areas.Api.Models.TipoIdentificacion>

@{
    ViewBag.Title = "Index";
}

<h2>Index</h2>

<p>
    @Html.ActionLink("Create New", "Create")
</p>
<table>
    <tr>
        <th>
            @Html.DisplayNameFor(model => model.tide_nombre)
        </th>
        <th>
            @Html.DisplayNameFor(model => model.est_codigo)
        </th>
        <th></th>
    </tr>

@foreach (var item in Model) {
    <tr>
        <td>
            @Html.DisplayFor(modelItem => item.tide_nombre)
        </td>
        <td>
            @Html.DisplayFor(modelItem => item.est_codigo)
        </td>
        <td>
            @Html.ActionLink("Edit", "Edit", new { id=item.tipoidentificacionId }) |
            @Html.ActionLink("Details", "Details", new { id=item.tipoidentificacionId }) |
            @Html.ActionLink("Delete", "Delete", new { id=item.tipoidentificacionId })
        </td>
    </tr>
}

</table>
